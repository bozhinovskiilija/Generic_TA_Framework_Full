name: Execute E2E Tests

on:

  workflow_dispatch:
    inputs:
 


jobs:
  release:
    name: Execute E2E tests
    runs-on: ubuntu-latest

    steps:
      
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up JDK 17
        uses: actions/setup-java@v2
        with:
          java-version: 17
          distribution: 'adopt'
          cache: 'maven'
      - name:  Install Google Chrome # Using shell script to install Google Chrome
        run: |
              chmod +x ./scripts/InstallChrome.sh ./scripts/InstallChrome.sh

      - name: Run tests from maven
        run: |
          mvn clean test 
 

     
